<%= form_for(ticket_loop) do |f| %>
  <% if ticket_loop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_loop.errors.count, "error") %> prohibited this ticket_loop from being saved:</h2>

      <ul>
      <% ticket_loop.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
